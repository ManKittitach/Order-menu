<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สั่งอาหาร - Meeting Reunion Coach #1</title>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-blue: #4A90E2;
            --light-blue: #E1F0FF;
            --primary-green: #8CC63F;
            --soft-pink: #FF8DA1;
            --text-dark: #333333;
            --white: #FFFFFF;
        }

        body {
            font-family: 'Kanit', sans-serif;
            background: linear-gradient(135deg, var(--light-blue) 0%, #ffffff 100%);
            color: var(--text-dark);
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .form-container {
            background-color: var(--white);
            width: 100%;
            max-width: 600px;
            border-radius: 24px;
            box-shadow: 0 10px 30px rgba(74, 144, 226, 0.15);
            overflow: hidden;
            border: 2px solid var(--light-blue);
        }

        .form-header {
            background: var(--primary-blue);
            color: var(--white);
            padding: 30px 20px;
            text-align: center;
            border-bottom: 5px solid var(--soft-pink);
        }

        .form-header h1 {
            margin: 0;
            font-size: 26px;
            font-weight: 600;
        }

        .form-header p {
            margin: 10px 0 0 0;
            font-size: 16px;
            font-weight: 300;
        }

        .form-body {
            padding: 25px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label.section-label {
            display: block;
            margin-bottom: 10px;
            font-weight: 600;
            font-size: 18px;
            color: var(--primary-blue);
            border-bottom: 2px dashed var(--light-blue);
            padding-bottom: 5px;
        }

        input[type="text"],
        input[type="tel"],
        textarea {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            font-family: 'Kanit', sans-serif;
            font-size: 15px;
            box-sizing: border-box;
            transition: 0.3s;
        }

        input[type="text"]:focus, input[type="tel"]:focus, textarea:focus {
            outline: none;
            border-color: var(--primary-blue);
        }

        /* ส่วนแสดงรูปภาพและคำเตือน */
        .menu-image-container {
            text-align: center;
            margin-bottom: 15px;
            background-color: #f8fafc;
            padding: 10px;
            border-radius: 16px;
            border: 1px solid #e2e8f0;
        }

        .menu-image-container img {
            width: 100%;
            height: auto;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        }

        .disclaimer {
            font-size: 12px;
            color: #e74c3c; /* สีแดงให้เห็นชัด */
            margin-top: 8px;
            margin-bottom: 5px;
        }

        /* ตกแต่งตัวเลือกลิสต์ */
        .radio-list {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .radio-list label {
            display: flex;
            align-items: center;
            padding: 12px 15px;
            background-color: #f8fafc;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            cursor: pointer;
            transition: 0.3s;
            font-weight: 400;
        }

        .radio-list label:hover {
            background-color: var(--light-blue);
            border-color: var(--primary-blue);
        }

        .radio-list input[type="radio"] {
            margin-right: 12px;
            transform: scale(1.3);
            accent-color: var(--primary-blue);
        }

        .drink-group {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
        }

        .submit-btn {
            background-color: var(--primary-blue);
            color: var(--white);
            border: none;
            width: 100%;
            padding: 15px;
            border-radius: 12px;
            font-size: 18px;
            font-weight: 600;
            font-family: 'Kanit', sans-serif;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 10px;
            box-shadow: 0 4px 6px rgba(74, 144, 226, 0.3);
        }

        .submit-btn:hover {
            background-color: var(--primary-green);
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(140, 198, 63, 0.4);
        }

        .submit-btn:disabled {
            background-color: #cbd5e1;
            cursor: not-allowed;
            box-shadow: none;
            transform: none;
        }

    </style>
</head>
<body>

    <div class="form-container">
        <header class="form-header">
            <h1>🍴 เลือกเมนูอาหาร & เครื่องดื่ม</h1>
            <p>กองทัพต้องเดินด้วยท้อง... เลือกรสชาติที่ใช่ได้เลย!</p>
        </header>

        <form class="form-body" id="foodForm">
            
            <div class="form-group">
                <label class="section-label">ข้อมูลส่วนตัว</label>
                <input type="text" name="fullname" placeholder="ชื่อ - นามสกุล" style="margin-bottom: 10px;" required>
                <input type="tel" name="phone" placeholder="เบอร์โทรศัพท์" required>
            </div>

            <div class="form-group">
                <label class="section-label">เลือกเซ็ตอาหาร (1 ท่าน / 1 เซ็ต)</label>
                
                <div class="menu-image-container">
                    <img src="pic-menu.png" alt="ภาพเมนูอาหาร">
                    <p class="disclaimer">*** เป็นเพียงภาพประกอบเพื่อตัดสินใจ อาจคลาดเคลื่อนจากของจริงบางส่วน***</p>
                </div>

                <div class="radio-list">
                    <label>
                        <input type="radio" name="foodSet" value="Set 1: ไก่ทอดกระเทียม + ต้มยำทะเลน้ำข้น" required>
                        Set 1: ไก่ทอดกระเทียม + ต้มยำทะเลน้ำข้น
                    </label>
                    <label>
                        <input type="radio" name="foodSet" value="Set 2: กระเพราเนื้อริปอาย + กะหล่ำปลีน้ำปลาหมูกรอบ">
                        Set 2: กระเพราเนื้อริปอาย + กะหล่ำปลีน้ำปลาหมูกรอบ
                    </label>
                    <label>
                        <input type="radio" name="foodSet" value="Set 3: กระเพราทะเล + ผัดผักรวม">
                        Set 3: กระเพราทะเล + ผัดผักรวม
                    </label>
                    <label>
                        <input type="radio" name="foodSet" value="Set 4: คะน้าหมูกรอบ + ลาบปลากระพงทอดกรอบ">
                        Set 4: คะน้าหมูกรอบ + ลาบปลากระพงทอดกรอบ
                    </label>
                    <label>
                        <input type="radio" name="foodSet" value="Set 5: ข้าวผัดทะเล + แกงเขียวหวานหมู">
                        Set 5: ข้าวผัดทะเล + แกงเขียวหวานหมู
                    </label>
                    <label>
                        <input type="radio" name="foodSet" value="Set 6: ทะเลคั่วพริกเกลือ + ต้มยำไก่น้ำข้น">
                        Set 6: ทะเลคั่วพริกเกลือ + ต้มยำไก่น้ำข้น
                    </label>
                </div>
            </div>

            <div class="form-group">
                <label class="section-label">เลือกเครื่องดื่ม</label>
                <div class="drink-group radio-list">
                    <label><input type="radio" name="drink" value="ชามะนาว" required> 🍋 ชามะนาว</label>
                    <label><input type="radio" name="drink" value="ชาไทย"> 🧋 ชาไทย</label>
                    <label><input type="radio" name="drink" value="อเมริกาโน่"> ☕ อเมริกาโน่</label>
                    <label><input type="radio" name="drink" value="ลาเต้"> 🥛 ลาเต้</label>
                </div>
            </div>

            <div class="form-group">
                <label class="section-label">หมายเหตุอื่นๆ</label>
                <textarea name="note" rows="3" placeholder="เช่น ขอรสจัดๆ เผ็ดๆ หรือแพ้อาหารทะเล พิมพ์บอกได้เลยครับ"></textarea>
            </div>

            <button type="submit" class="submit-btn" id="submitBtn">บันทึกออเดอร์ 🍽️</button>
            
        </form>
    </div>

    <script>
        document.getElementById('foodForm').addEventListener('submit', function(e) {
            e.preventDefault(); 
            
            const btn = document.getElementById('submitBtn');
            const originalText = btn.innerText;
            btn.innerText = 'กำลังบันทึกออเดอร์...';
            btn.disabled = true;

            const formData = new URLSearchParams(new FormData(this));

            // ⚠️ นำ URL จาก Apps Script มาใส่ตรงนี้
            const WEB_APP_URL = 'ใส่_URL_ที่ได้จาก_Google_Apps_Script_ไฟล์ใหม่_ตรงนี้';

            fetch(WEB_APP_URL, {
                method: 'POST',
                body: formData,
                mode: 'no-cors'
            })
            .then(() => {
                alert('บันทึกเมนูอาหารเรียบร้อยแล้ว เตรียมตัวอร่อยได้เลย! 🎉');
                this.reset();
            })
            .catch(error => {
                alert('เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง');
            })
            .finally(() => {
                btn.innerText = originalText;
                btn.disabled = false;
            });
        });
    </script>

</body>
</html>
